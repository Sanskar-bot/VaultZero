/**
 * JWT Authentication Middleware
 *
 * Verifies the Authorization: Bearer <token> header on protected routes.
 * Extracts userId from the JWT payload and attaches it to req.user.
 *
 * Implementation: Day 4
 */

// TODO: Day 4 — implement authenticateJWT middleware
