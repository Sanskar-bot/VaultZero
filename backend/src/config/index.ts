/**
 * Backend Configuration
 *
 * Centralizes all environment variable reading with validation.
 * Uses zod to validate and parse env vars at startup so we fail
 * fast if anything is missing.
 *
 * Implementation: Day 4
 */

// TODO: Day 4 — implement config schema and export validated config
