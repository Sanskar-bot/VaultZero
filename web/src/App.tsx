/**
 * VaultZero Web — App Component
 *
 * Pages:
 * - /login        — email + master password login
 * - /register     — registration with recovery phrase display
 * - /vault        — main vault list (protected)
 * - /settings     — account settings, device list, audit log
 * - /recovery     — recovery phrase entry
 *
 * Implementation: Day 6
 */

// TODO: Day 6 — implement App with React Router
