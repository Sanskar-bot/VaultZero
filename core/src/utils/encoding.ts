/**
 * Encoding Utilities — Base64, Hex, UTF-8 ↔ Uint8Array conversions
 *
 * These are used throughout VaultZero to convert between:
 * - Uint8Array (what crypto APIs produce/consume)
 * - Base64 strings (what we store/transmit)
 * - UTF-8 strings (what we JSON.stringify for vault contents)
 *
 * Implementation: Day 2
 */

// TODO: Day 2 — implement toBase64(data: Uint8Array) → string
// TODO: Day 2 — implement fromBase64(str: string) → Uint8Array
// TODO: Day 2 — implement toHex(data: Uint8Array) → string
// TODO: Day 2 — implement fromHex(str: string) → Uint8Array
// TODO: Day 2 — implement utf8Encode(str: string) → Uint8Array
// TODO: Day 2 — implement utf8Decode(data: Uint8Array) → string

export {};
