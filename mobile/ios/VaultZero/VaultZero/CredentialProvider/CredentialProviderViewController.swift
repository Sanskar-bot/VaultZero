/**
 * VaultZero iOS — CredentialProviderViewController (placeholder)
 *
 * Implements ASCredentialProviderExtensionContext for iOS AutoFill:
 * - Responds to ASCredentialProviderExtensionContext requests
 * - Matches serviceIdentifier (domain) to vault entries
 * - Requires LocalAuthentication (Face ID / Touch ID) before providing credential
 *
 * SECURITY:
 * - Credential is provided through the secure ASCredentialProviderExtensionContext
 * - Never exposes raw passwords to the host app
 *
 * Implementation: Day 12-13
 */

// TODO: Day 12-13 — implement CredentialProviderViewController
